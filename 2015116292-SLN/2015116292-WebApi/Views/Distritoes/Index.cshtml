@model IEnumerable<_2015116292_ENT.Entities.Distrito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Distrito_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Distrito_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Distrito_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Distrito_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Distrito_id })
        </td>
    </tr>
}

</table>
