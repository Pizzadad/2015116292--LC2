@model IEnumerable<_2015116292_ENT.Entities.AdministradorEquipo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._Equipocelular.Equipocelular_modelo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdministradorEquipo_modelo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._Equipocelular.Equipocelular_modelo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdministradorEquipo_modelo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdministradorEquipo_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdministradorEquipo_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdministradorEquipo_id })
        </td>
    </tr>
}

</table>
