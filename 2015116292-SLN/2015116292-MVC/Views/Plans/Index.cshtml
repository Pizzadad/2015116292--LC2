@model IEnumerable<_2015116292_ENT.Entities.Plan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._TipoPlan.TipoPlan_caracteristica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plan_descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._TipoPlan.TipoPlan_caracteristica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Plan_descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Plan_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Plan_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Plan_id })
        </td>
    </tr>
}

</table>
