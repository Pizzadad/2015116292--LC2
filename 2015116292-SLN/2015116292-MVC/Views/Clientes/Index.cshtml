@model IEnumerable<_2015116292_ENT.Entities.Cliente>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Cliente_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Cliente_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Cliente_id })
        </td>
    </tr>
}

</table>
