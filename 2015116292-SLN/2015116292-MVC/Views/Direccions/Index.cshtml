@model IEnumerable<_2015116292_ENT.Entities.Direccion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._Ubigeo.Ubigeo_numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Direccion_lugar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._Ubigeo.Ubigeo_numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Direccion_lugar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Direccion_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Direccion_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Direccion_id })
        </td>
    </tr>
}

</table>
