@model IEnumerable<_2015116292_ENT.Entities.TipoPlan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoPlan_caracteristica)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoPlan_caracteristica)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoPlan_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoPlan_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoPlan_id })
        </td>
    </tr>
}

</table>
